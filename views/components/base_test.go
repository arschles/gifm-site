package components

import (
	"testing"
)

func TestBase(t *testing.T) {
	// base := Base(emptyElt{})
	// var b bytes.Buffer
	// if err := base.Render(&b, emptyData()); err != nil {
	// 	t.Fatal(err)
	// }
	// t.Logf(string(b.Bytes()))
}
